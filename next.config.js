/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/IDEAlg',
  assetPrefix: '/IDEAlg',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
